# Scripts to skip during automated runs (smoke tests, pre-release checks, CI).
# Each entry is matched against script paths:
#   - Entries with '/' do a substring match against the file path
#   - Entries without '/' match the file stem exactly
# Add an inline # comment to document the reason for skipping.
#
# SLOW-skip convention:
#   Entries tagged `# SLOW <YYYY-MM-DD> - <reason>` mark scripts that are
#   skipped because they exceed the per-script timeout cap (300s by
#   default; 1800s for mode=release runs). These are
#   NOT permanent skips — every mega-run surfaces them with a loud warning
#   banner. Fix the performance issue and remove the SLOW marker.
#
# NEEDS_FIX convention:
#   Entries tagged `# NEEDS_FIX <YYYY-MM-DD> - <reason>` mark scripts that
#   are broken and parked as a to-do list. Like SLOW-skips, these are NOT
#   permanent skips — every mega-run surfaces them with a loud warning
#   banner. Investigate the failure, fix the underlying bug, and remove
#   the NEEDS_FIX marker.

- session/general # Session mostly works but not maintaining currently.
- session/multi_analysis # Session mostly works but not maintaining currently.
- graphical/ep.py # NEEDS_FIX 2026-08-03 - EP not release-ready: the per-factor searches are truncated (maxcall/maxiter=1000), so each search projects an ESS=1 posterior whose weighted variance is exactly 0; EP feeds that back as a delta-function prior, and the next cycle's initializer raises InitializerException. Intermittent - broke the 2026-08-03 nightly integrate. See PyAutoFit #1332 F10.
- graphical/ep_deterministic.py # NEEDS_FIX 2026-08-03 - EP parked as not release-ready alongside graphical/ep.py. Passes today, but shares the EP machinery whose message projection is unstable. See PyAutoFit #1332 F10.
- graphical/ep_exact.py # NEEDS_FIX 2026-08-03 - EP parked as not release-ready alongside graphical/ep.py. Passes today, but shares the EP machinery whose message projection is unstable. See PyAutoFit #1332 F10.
- graphical/ep_parity.py # NEEDS_FIX 2026-08-03 - EP parked as not release-ready alongside graphical/ep.py. Passes today, but shares the EP machinery whose message projection is unstable. See PyAutoFit #1332 F10.
- graphical/analytic_gaussian.py # NEEDS_FIX 2026-09-02 - autofit-EP column fails the closed form on an exactly Gaussian graph (D1 id-0 prior/FactorValue collision, D2/D3 Laplace projection); closed form, minimal EP and graphical joint fit agree. Re-enable when autofit_workspace_test#91's PyAutoFit prompts land. See PyAutoFit #1405.
- graphical/analytic_gaussian_priors.py # NEEDS_FIX 2026-09-02 - autofit-EP column fails the closed form for every hyper-prior family (D2 Laplace covariance, D4 truncation limits dropped, D5 log-space transform lost in projection); minimal EP matches the closed form. Re-enable when autofit_workspace_test#91's PyAutoFit prompts land. See PyAutoFit #1405.
- graphical/analytic_gaussian_collapse.py # NEEDS_FIX 2026-09-02 - phase-2 collapse configuration fails on silent STALE / BIASED-TIGHT scatter with no library warning (D2 Laplace covariance, D3 failed line search still projects, D4 truncation limits dropped); closed form is exact. Re-enable when autofit_workspace_test#91's PyAutoFit prompts land. See PyAutoFit #1405.
